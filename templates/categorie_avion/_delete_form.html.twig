<form method="post" action="{{ path('app_categorie_avion_delete', {'id': categorie_avion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_avion.id) }}">
    <button class="btn">Delete</button>
</form>
