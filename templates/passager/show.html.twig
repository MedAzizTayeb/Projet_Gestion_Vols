{% extends 'base.html.twig' %}

{% block title %}Passager{% endblock %}

{% block body %}
    <h1>Passager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ passager.id }}</td>
            </tr>
            <tr>
                <th>NumPassport</th>
                <td>{{ passager.numPassport }}</td>
            </tr>
            <tr>
                <th>Nationalite</th>
                <td>{{ passager.nationalite }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ passager.dateNaissance ? passager.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>BesoinsSpeciaux</th>
                <td>{{ passager.besoinsSpeciaux }}</td>
            </tr>
            <tr>
                <th>PoidsBagages</th>
                <td>{{ passager.poidsBagages }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_passager_index') }}">back to list</a>

    <a href="{{ path('app_passager_edit', {'id': passager.id}) }}">edit</a>

    {{ include('passager/_delete_form.html.twig') }}
{% endblock %}
